import 'package:flutter/material.dart';

class AuthorViewLoading extends StatelessWidget {
  const AuthorViewLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
