import 'package:flutter/material.dart';

class PlaylistViewLoading extends StatelessWidget {
  const PlaylistViewLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
